{
    "configurations": [
    {
        "name": "Debug",
        "type": "lldb",
        "request": "launch",
        "program": "${workspaceFolder}/samples_debug",
        "stdio": [],
        "cwd": "${fileDirname}",
        "env": {
      "ASAN_OPTIONS": "abort_on_error=1:detect_leaks=0"
        }
    },
    ]
}